# Keep line endings the same on every computer. Text files are stored with LF in git.
* text=auto eol=lf
# Windows batch files need CRLF to run reliably.
*.bat text eol=crlf
# Never try to diff or convert these.
*.ttf binary
*.png binary
*.zip binary
*.ico binary
*.icns binary
