[pytest]
# The browser tests take a few minutes and need Chrome, so they are skipped unless asked for:
#   python -m pytest -m browser
addopts = -m "not browser"
markers =
    browser: plays the real app in headless Chrome (slow; needs Chrome)
